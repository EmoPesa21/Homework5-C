#include <stdio.h>
int main()
{
	int arr[20] = {10,2,3,14,15,16,12,72,23,12,4,51,23,1,2,6,7,64,111,124};
		int i;
		int count = 0;
		for(i=0; i<20;i++)
			{
				if(arr[i] >=9 && arr[i] <= 99)
					{
						count +=1;
					}
			}
			printf("There are %d instances in the num: ", count);
return 0;
}
/*
OUTPUT TERMINAL:
There are 11 instances in the num:
*/
